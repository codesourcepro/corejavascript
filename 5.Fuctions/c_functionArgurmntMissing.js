/**
 Function Argument Missing in JavaScript
 => If a function is called with missing arguments, the values are set to undefined.

 */

function add(a, b, c) {
    console.log("A: " + a + "B: " + b + "C: " + c);
}

add(10, 20, 30)